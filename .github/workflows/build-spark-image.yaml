name: Build Spark Image

on:
  # schedule:           # 注释掉或删除此部分以停止自动触发
  #   - cron: '0 3 * * *'
  workflow_dispatch:    # 仅保留手动触发

env:
  IMAGE_NAME: parallax
  NAMESPACE: gradientservice

jobs:
  build:
    runs-on: arc-public-parallax
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    strategy:
      matrix:
        variant: [spark]

    steps:
      - name: Workflow disabled – no actions performed
        run: |
          echo "=================================================="
          echo "This workflow has been DISABLED."
          echo "No Docker images are built or pushed."
          echo "=================================================="
